import React from 'react';
import '../App.css';

const Home = () => {
	return (
		<div>
			<p className='hometext'>
				This is a simple project using React to create a stopwatch and a timer.
				Start by clicking the "Stopwatch" or "Timer" button above
			</p>
		</div>
	);
};

export default Home;
