import React, { useState, useEffect } from 'react';
import './../App.css';

function FunctionalClock(props) {
	//
	const [startTime, setInitialClock] = useState(new Date());
	const [stopWatchTime, setStopWatchTime] = useState(new Date());
}

export default FunctionalClock;
